{
  "name": "uniconnect",
  "version": "1.0.0",
  "description": "UniConnect - Educational Platform",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "setup": "npm.cmd install && cd backend && npm.cmd install && cd ../frontend && npm.cmd install",
    "build": "cd frontend && npm run build",
    "lint": "cd frontend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}