import api from './api';

// User API service

const userService = {
  getProfile: async (userId) => {
    // Implementation
  },

  updateProfile: async (userId, userData) => {
    // Implementation
  },

  getAllUsers: async () => {
    // Implementation
  }
};

export default userService;
